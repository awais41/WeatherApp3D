class ApiConstants {
  static const String baseUrl = 'https://';
  static const String weatherEndpoint = '/weather';
  // If your server later requires an API key, add it here and include in requests.
}
